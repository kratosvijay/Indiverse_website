import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Trigger Vercel rebuild for custom domain routing sync
  // Trigger 2: apply disabled deployment protection settings
};

export default nextConfig;
